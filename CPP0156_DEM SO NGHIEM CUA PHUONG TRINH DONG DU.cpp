#include<bits/stdc++.h>

using namespace std;
using ll = long long;

int main()
{
    int t; cin >> t;
    while(t--)
    {
       int b, p; cin >> b >> p;
       for(int i = 1; i <= b; i++)
       {
            if(1 % p == i  )
       }
    }
}
